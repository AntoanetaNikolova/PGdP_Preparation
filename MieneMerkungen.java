import java.math.BigDecimal;
import java.math.RoundingMode;
import java.text.DecimalFormat;

public class MieneMerkungen {
    //ToDo: print an array:
    /*
        for(int i = 0; i < a.length; i++){
			System.out.println(Arrays.toString(a[i]));
		}
     */

    //ToDo: eine double Zahl runden;
    //DecimalFormat format = new DecimalFormat("#.##");
    //System.out.println(a1 + " wird in: "+ format.format(a1));

    //ToDo: eine double Zahl abschneiden;
    //BigDecimal bd = new BigDecimal(String.valueOf(a1));
    //BigDecimal rounded = bd.setScale(2, RoundingMode.FLOOR);
    //    System.out.println(a1 + " wird in: "+ rounded);

    public static void main(String[] args) {
        //Trying some stuff out;
    }



}
